" Simple Vim Configuration

" display command as it's type in the bottom-right corner
set showcmd

" show the line and column number of the cursor position
set ruler

" turn on line numbers
set number

" don't wrap lines mid-word
set lbr

" set indentation to the same as current line
set autoindent

" turn on smart auto-indenting
set smartindent

" set tab width to 2 spaces
set shiftwidth=2
set tabstop=2

" set colour scheme
syntax on 
colorscheme old-hope